using System;
using UnityEngine;

namespace Model {
    [Serializable]
    public enum ColorType {
        @default, // white in most cases
        red,
        blue,
        green,
        yellow,
        orange,
        cyan,
        magenta
    }
}
